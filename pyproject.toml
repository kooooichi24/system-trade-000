[project]
name = "system-trading-learning"
version = "0.1.0"
description = "System trading learning starter (uv)"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
stl = "system_trading_learning.cli:main"

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/system_trading_learning"]
